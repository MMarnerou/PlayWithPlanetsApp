<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mariamarnerou.testapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/letsgo"
        android:label="Play with Planets"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Modes" />
        <activity android:name=".StudyMode" />
        <activity android:name=".GameMode" />
        <activity android:name=".PlanetQuiz" />
        <activity android:name=".FinalQuiz" />
        <activity android:name=".NewAccount"></activity>
    </application>

</manifest>